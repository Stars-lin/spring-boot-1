<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dzkj.mapper.IOrdertotalMapper">
  <update id="updstates">
   update ordertotal 
   set t_state=1,
   a_consigneename=#{a_consigneename},
   a_province=#{a_province},
   a_city=#{a_city},
   a_area=#{a_area},
   a_details=#{a_details},
   a_phone=#{a_phone}
   where o_bh=#{o_bh}
  </update> 
</mapper>